const Temp = () => {
  /*const state = {
    contacts: [
      { id: 1, b: 2 },
      { id: 2, b: 2 },
      { id: 3, b: 2 },
    ],
  };

  console.log({
    contacts: state.contacts.filter((contact) => contact.id !== 2),
  });*/
  return <></>;
};

export default Temp;
